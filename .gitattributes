# Handle line endings automatically for files detected as text and leave all files detected as
# binary untouched.
* text=auto

# These files are text and should be normalized (Convert crlf => lf)
*.factories text
*.java text
*.md text
*.xml text

# These files are binary and should be left untouched (binary is a macro for -text -diff)
*.jar binary

# Windows
*.bat text eol=crlf
*.cmd text eol=crlf

# Unix/Linux
*.sh text eol=lf

